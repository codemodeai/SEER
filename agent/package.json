{
  "name": "seer-agent",
  "version": "1.0.0",
  "description": "SEER Local Agent — runs as Tauri sidecar, executes Claude tasks locally",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js",
    "start": "node dist/index.js",
    "build:exe": "node scripts/build-exe.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
